// The live proxy host, shared by docs/landing/onboarding so it can never drift.
// Swap to a vanity domain (api.allowance.dev) once owned.
export const PROXY_URL = "https://api-wallet-proxy.6rataq.workers.dev";
