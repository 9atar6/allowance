// Test stub for the `server-only` package. In a real Next build this guard
// throws if imported into a client bundle; under Vitest (node) we no-op it so
// server-only modules (e.g. lib/keys.ts) can be unit-tested.
export {};
